name: "Primer Action"

#Trigger
on:
  push:
    branches: ["feature/clase-2"]

jobs:
  deploy-primer-action_first_job:
    runs-on: ubuntu-latest #Runners
    steps:
      - run: echo "Hacemos pull del código"
      - run: echo "Correar migraciones"

  deploy-primer-action_second_job:
    if: ${{ github.ref == 'refs/heads/feature/clase-1' }}
    needs: deploy-primer-action_first_job
    runs-on: ubuntu-latest #Runners
    steps:
      - run: echo "Generar binario"
      - run: echo "Publicar version"
